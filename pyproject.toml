[project]
name = "remake-slides-python-tools"
version = "0.1.0"
description = "Python tooling for PPTX analysis and Marp slide workflows."
requires-python = ">=3.11"
dependencies = [
  "pillow>=11.3.0",
  "python-pptx>=1.0.2",
]

[tool.uv]
package = false
