<figure
  className={`m-0 flex flex-col gap-1 overflow-hidden rounded-lg border border-[rgba(23,35,59,0.14)] shadow-[0_18px_46px_rgba(19,40,75,0.14)] ${props.className || ""}`}
  style={{ backgroundColor: props.bgColor || "#f3efe7" }}
>
  <div className="min-h-0 flex-1 overflow-hidden">
    <img
      className={`${props.contain === false ? "object-cover object-center" : "object-contain object-center"} block h-full w-full ${props.imageClassName || ""}`}
      src={props.src}
      alt={props.alt || props.caption || ""}
    />
  </div>
  {props.caption ? (
    <figcaption className="px-2 pb-1 text-center text-[13px] font-semibold leading-tight text-[var(--muted)]">
      {props.caption}
    </figcaption>
  ) : null}
</figure>
